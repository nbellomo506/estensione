<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<VBox alignment="CENTER" spacing="10" stylesheets="@application.css" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="application.SceltaController">
   
    <TableView fx:id="dendrogramTable" styleClass="dendrogram-table" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="dendrogramColumn" prefWidth="89.0" text="Dendrogramma" />
        </columns>
    </TableView>
    
    <!-- Componenti per File -->
    <VBox fx:id="fileComponents" spacing="10" styleClass="input-container">
        <HBox alignment="CENTER" spacing="10">
            <TextField fx:id="fileNameInput" promptText="Nome file (con estensione)" styleClass="input-field" HBox.hgrow="ALWAYS" />
            <Button fx:id="loadFileButton" onAction="#handleLoadFile" styleClass="action-button" text="Carica File" />
        </HBox>
    </VBox>
    
    <!-- Componenti per Database -->
    <VBox fx:id="dbComponents" spacing="10" styleClass="input-container">
        <HBox alignment="CENTER" spacing="10">
            <TextField fx:id="depthInput" prefWidth="100" promptText="Profondità" styleClass="input-field" />
            <ComboBox fx:id="distanceType" promptText="Tipo distanza" styleClass="combo-box" />
            <Button fx:id="executeButton" onAction="#handleExecute" styleClass="action-button" text="Esegui" />
        </HBox>
        <HBox alignment="CENTER" spacing="10">
            <TextField fx:id="saveFileInput" promptText="Nome file per salvataggio" styleClass="input-field" HBox.hgrow="ALWAYS" />
            <Button fx:id="saveButton" onAction="#handleSave" styleClass="action-button" text="Salva" />
        </HBox>
    </VBox>
    
    <padding>
        <Insets bottom="10" left="10" right="10" top="10" />
    </padding>
</VBox>
